#!/bin/sh

ps cax | grep motion > /dev/null
if [ $? -eq 0 ]; then
    echo "Motion is already running."
    exit 1
fi

mkdir -p $HOME/.log
mkdir -p $HOME/webcam
motion -n -c $HOME/.config/motion/motion.conf -p /tmp/motion.pid >> $HOME/.log/motion.log 2>&1 &
